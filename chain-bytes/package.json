{
  "name": "chain-bytes",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ chain-bytes",
    "create-local": "graph create --node http://localhost:8020/ chain-bytes",
    "remove-local": "graph remove --node http://localhost:8020/ chain-bytes",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chain-bytes",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.4",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
